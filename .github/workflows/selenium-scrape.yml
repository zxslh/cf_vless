name: Selenium Scrape Cloudflare IPs
on:
  workflow_dispatch:
  #schedule:
   # - cron: '*/15 * * * *'

jobs:
  scrape-cloudflare-ips:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Set up Python environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Cache Python dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip show selenium webdriver-manager
          echo "Python依赖安装完成"

      # 核心修改：安装webdriver-manager支持的Chrome版本（127.0.6533.88）
      - name: Install compatible Chrome browser
        run: |
          # 卸载旧版Chromium
          sudo apt-get remove -y chromium-browser chromium-browser-l10n chromium-codecs-ffmpeg
          sudo apt-get autoremove -y
          # 添加Chrome官方源
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update -y
          # 安装127.0.6533.88（已验证：webdriver-manager可自动匹配对应驱动）
          sudo apt-get install -y google-chrome-stable=127.0.6533.88-1
          # 验证版本
          google-chrome --version
          echo "Chrome浏览器安装完成（版本：127.0.6533.88）"

      - name: Run Selenium scrape script
        run: |
          python scrape_ips.py
          echo "爬虫脚本执行完成"

      - name: Upload scrape result
        uses: actions/upload-artifact@v4
        with:
          name: cloudflare-ips-${{ github.run_id }}
          path: ips_result.txt
          retention-days: 7
          if-no-files-found: error
